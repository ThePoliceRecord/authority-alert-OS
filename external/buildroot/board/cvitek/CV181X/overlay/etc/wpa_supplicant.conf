ctrl_interface=/var/run/wpa_supplicant
update_config=1

# Disable automatic background scanning to prevent disconnections
# Scans will only happen when explicitly triggered via wpa_cli
bgscan=""
autoscan=""

# Disable power save mode which can cause disconnects
ap_scan=1

# Disable fast transition to prevent reconnection issues
fast_reauth=0
